pub fn search_insert(nums: Vec<i32>, target: i32) -> i32 {
    return 0;
}

fn main() {
    let nums = vec![1,3,5,6];
    let target = 5;
    let x = search_insert(nums, target);
    println!("Answer: {}",x);
}
